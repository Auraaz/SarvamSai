(function () {
  window.SARVAMSAI_LAUNCH_CONFIG = {
    // Optional — override API root (no trailing slash after …/api).
    // Default in launch-system.js is https://api.sarvamsai.in/api. Uncomment only for fallback:
    // API_BASE: "https://sarvamsai-api.onrender.com/api",
  };
})();
